[project]
name = "gcode-lib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "corvinkoegler", email = "corvin.koegler.hpi@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.24,<0.9.0"]
build-backend = "uv_build"
